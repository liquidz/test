name: test1
on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - run: echo "hello=world" >> $GITHUB_ENV
      - run: echo "wani" >> $GITHUB_ENV
      - run: echo "oni" >> $GITHUB_ENV
      - run: echo "neko=inu" >> $GITHUB_ENV
      - run: echo "answer = ${{ env.neko }}"
