name: test1
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: clojure:openjdk-14-tools-deps
    steps:
      - uses: actions/checkout@v2
      - run: ls -al ~
      - name: Cache cpcache
        uses: actions/cache@v1.1.0
        with:
          path: .cpcache
          key: load-files-cpcache-${{ hashFiles('deps.edn') }}

      - run: clojure -m foo.core
      - run: ls -al
      - run: ls -al ~
