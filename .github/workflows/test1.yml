name: test1
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo hello
      - name: Foooo
        uses: ./
        with:
          classpath: 'foo bar'
          namespaces: 'hoge fuga'
