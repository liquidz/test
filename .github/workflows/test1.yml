name: test1
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo hello
      - name: Babashka test
        uses: ./
        with:
          classpath: 'src test'
          namespaces: 'foo.core-test'
